/** * Created by pengyao on 16/5/28. */import React from "react";import TopicEditor from "./TopicEditor"import {addTopic} from "../lib/client";export default class AddTopic extends React.Component{    constructor(props) {        super(props);        this.state = {}    }    render(){        return (            <TopicEditor topic={null} title="发表新帖子" onSave={(topic,done)=>{                addTopic(topic.title,topic.tags,topic.content)            .then(ret=>{                done();                location = `topic/${ret.topic._id}`            })            .catch(err=>{                done();                alert(err);            })            }}            />        )    }}